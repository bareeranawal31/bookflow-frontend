{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/images/*.{png,jpg,gif}", "/css/*", "*.js"]
  },
  "globalHeaders": {
    "cache-control": "no-cache"
  },
  "routes": [
    {
      "route": "/api/*",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"],
      "rewrite": "https://bookflow-api-h4afdpc5cwgqgdch.azurewebsites.net/api/*",
      "allowedRoles": ["anonymous"]
    }
  ]
}
